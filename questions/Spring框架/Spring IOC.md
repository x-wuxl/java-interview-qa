# Spring IOC

1. 介绍一下Spring的IOC
2. SpringBean的生命周期是怎么样的？
3. SpringBean的初始化过程是怎么样的？
4. Spring中的Bean作用域有哪些？
5. Spring中的Bean是线程安全的吗？
6. Spring默认支持循环依赖吗？如果发生如何解决？
7. 什么是Spring的循环依赖问题？
8. 什么是Spring的三级缓存
9. 三级缓存是如何解决循环依赖的问题的？
10. Spring解决循环依赖一定需要三级缓存吗？
11. @Lazy注解能解决循环依赖吗？
12. Spring IOC AOP
13. Spring循环依赖，如何解决
14. SpringBean生命周期
15. Spring Bean作用域：单例、原型、每一次HTTP请求都会产生一个新的bean、每一次来自新session的HTTP请求都会产生一个新的bean、每个Web应用在启动时创建一个Bean、每一次WebSocket会话产生一个新的bean

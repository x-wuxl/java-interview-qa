# 新增题目操作指南

本文档详细说明如何在首页列表中添加新题目，并确保题目按照"学习路径排序_优化版"中的顺序正确显示。

## 📋 操作流程概览

新增题目的完整流程包括以下3个步骤：

1. **在排序文件中添加题目** - 编辑 `_data/学习路径排序_优化版.md`
2. **创建文章文件** - 在对应的分类目录下创建 markdown 文件
3. **重新生成排序数据** - 运行脚本更新 `_data/home_order.yml`

---

## 步骤1：在排序文件中添加题目

### 1.1 打开排序文件

编辑文件：`_data/学习路径排序_优化版.md`

### 1.2 确定题目位置

根据题目的分类，找到对应的章节。例如：
- Java基础题目 → `## 一、Java基础（1-33）`
- MySQL题目 → `## 五、数据库MySQL（211-321）`
- 如果有子分类，找到对应的 `### 子分类标题（范围）`

### 1.3 添加题目条目

在合适的位置添加新题目，格式为：

```markdown
序号. 题目标题
```

**示例**：
```markdown
## 五、数据库MySQL（211-321）

### 5.1 索引原理（211-236）
...
236. MySQL选错索引的原因及解决方案
237. 新题目标题  ← 在这里添加新题目
```

### 1.4 更新范围数字

添加题目后，需要更新章节的范围数字。

**示例**：
- 原范围：`## 五、数据库MySQL（211-321）`
- 添加1题后：`## 五、数据库MySQL（211-322）`
- 子分类也需要更新：`### 5.1 索引原理（211-237）`

### 1.5 注意事项

- ✅ 序号必须连续，不能跳跃
- ✅ 题目标题必须与文章文件的 `title` 字段**完全一致**（包括标点符号、空格）
- ✅ 如果题目属于子分类，要添加到对应的子分类下
- ✅ 如果题目属于大分类，直接添加在大分类下

---

## 步骤2：创建文章文件

### 2.1 确定文件位置

根据题目分类，在对应的目录下创建文件：

| 分类 | 文件路径 |
|------|---------|
| Java基础 | `questions/Java基础/` |
| 集合框架 | `questions/集合框架/` |
| JVM | `questions/JVM/` |
| 并发编程 | `questions/并发编程/` |
| MySQL | `questions/MySQL/` |
| Redis | `questions/Redis/` |
| Spring框架 | `questions/Spring框架/` |
| 设计模式 | `questions/设计模式/` |
| 分布式 | `questions/分布式/` |
| 中间件 | `questions/中间件/` |

### 2.2 创建文件

文件名格式：`题目标题.md`（使用英文或拼音，避免特殊字符）

**示例**：
- 题目：`MySQL索引优化最佳实践`
- 文件名：`MySQL索引优化最佳实践.md` 或 `mysql-index-optimization-best-practices.md`

### 2.3 编写文章内容

文章必须包含正确的 Front Matter（文件头部元数据）：

```markdown
---
layout: post
title: "题目标题"  ← 必须与排序文件中的标题完全一致
date: 2025-11-02
categories: [分类名称]
tags: [标签1, 标签2]
description: "题目描述"
---

# 题目标题

文章内容...
```

**重要提示**：
- `title` 字段必须与 `学习路径排序_优化版.md` 中的标题**完全一致**
- 包括标点符号、空格、大小写都必须匹配
- 例如：排序文件中是 `ORDER BY是怎么实现的？`，文章中的 title 也必须是 `ORDER BY是怎么实现的？`

### 2.4 参考示例

查看现有文章作为参考：
- `_posts/2025-11-02-how-mysql-order-by-works.md`

---

## 步骤3：重新生成排序数据

### 3.1 运行生成脚本

在项目根目录下执行：

```bash
python scripts/generate_home_data.py
```

或者：

```bash
python3 scripts/generate_home_data.py
```

### 3.2 检查输出

脚本会输出生成结果：

```
Success: Generated _data/home_order.yml
   - Sections: 17
   - Questions: 462
```

### 3.3 验证结果

1. 检查 `_data/home_order.yml` 是否已更新
2. 确认新题目已出现在对应的分类和位置
3. 启动 Jekyll 服务器验证首页显示是否正确

---

## 🔍 完整示例

假设要添加一个新题目：**"MySQL索引失效的常见原因有哪些？"**

### 步骤1：编辑排序文件

在 `_data/学习路径排序_优化版.md` 中：

```markdown
## 五、数据库MySQL（211-321）

### 5.1 索引原理（211-236）
...
236. MySQL选错索引的原因及解决方案
237. MySQL索引失效的常见原因有哪些？  ← 新增
```

更新范围：
```markdown
## 五、数据库MySQL（211-322）  ← 更新范围
### 5.1 索引原理（211-237）  ← 更新范围
```

### 步骤2：创建文章文件

在 `questions/MySQL/` 目录下创建 `MySQL索引失效的常见原因有哪些？.md`：

```markdown
---
layout: post
title: "MySQL索引失效的常见原因有哪些？"
date: 2025-11-02
categories: [MySQL, 性能优化]
tags: [MySQL, 索引, 性能优化, 索引失效]
description: "深入解析MySQL索引失效的常见原因及解决方案"
---

# MySQL索引失效的常见原因有哪些？

文章内容...
```

### 步骤3：运行脚本

```bash
python scripts/generate_home_data.py
```

### 步骤4：验证

1. 检查 `_data/home_order.yml` 中是否包含新题目
2. 启动 Jekyll 查看首页是否正确显示

---

## ⚠️ 常见问题

### 问题1：题目在首页不显示

**可能原因**：
- 标题不匹配（最常见）
- 文章日期在未来
- 脚本未运行

**解决方法**：
1. 检查文章 `title` 与排序文件中的标题是否完全一致
2. 确保文章日期不是未来日期
3. 重新运行生成脚本

### 问题2：题目顺序错误

**可能原因**：
- 排序文件中的序号不连续
- 范围数字未更新

**解决方法**：
1. 检查排序文件中的序号是否连续
2. 更新章节的范围数字
3. 重新运行生成脚本

### 问题3：题目出现在错误的分类

**可能原因**：
- 题目添加到了错误的章节
- 文章文件的 `categories` 字段不正确

**解决方法**：
1. 检查排序文件中题目的位置
2. 检查文章文件的 `categories` 字段
3. 重新运行生成脚本

---

## 📝 检查清单

在完成新增题目后，请确认：

- [ ] 排序文件中已添加题目，序号连续
- [ ] 章节范围数字已更新
- [ ] 文章文件已创建，`title` 字段与排序文件完全一致
- [ ] 文章文件在正确的分类目录下
- [ ] 已运行生成脚本
- [ ] 检查了 `home_order.yml` 中的新条目
- [ ] 在首页验证了题目显示正确

---

## 🔗 相关文件

- 排序源文件：`_data/学习路径排序_优化版.md`
- 生成脚本：`scripts/generate_home_data.py`
- 排序数据：`_data/home_order.yml`（自动生成，勿手动编辑）
- 首页布局：`_layouts/home.html`
- 数据说明：`_data/README.md`

---

**最后更新**：2025年11月7日

